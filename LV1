/*짝수와 홀수
문제 설명
정수 num이 짝수일 경우 "Even"을 반환하고 홀수인 경우 "Odd"를 반환하는 함수, solution을 완성해주세요.

제한 조건
num은 int 범위의 정수입니다.
0은 짝수입니다.
입출력 예
*/
class Solution {
    public String solution(int num) {
        //n을 2로 나눈 나머지가 0인게 ? 참이면 짝수이므로 Even : 거짓이면 홀수이므로 Odd 반환
        return num%2==0 ? "Even" : "Odd";
        /* 같은걸 3항연산자가 아닌 if문으로 작성
        String answer = "";
        if(num%2==0){
          answer = Even;
        }else{
          answer = Odd;
        }
        
        */
    }
}
